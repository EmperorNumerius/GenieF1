# Backend API base URL. In dev, leave unset to default to http://localhost:8000.
# In production (Cloudflare Pages), set this to your deployed backend URL.
# Example: NEXT_PUBLIC_API_BASE=https://f1-api.numeri.us
NEXT_PUBLIC_API_BASE=
